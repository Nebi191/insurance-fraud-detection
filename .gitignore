__pycache__/
*.pyc
.ipynb_checkpoints/
.env
venv/
*.pkl

# NOT: `data/` ve `insurance_claims.csv` BİLEREK ignore edilmiyor.
# Gerekçe: `data/insurance_claims.csv` herkese açık bir Kaggle veri seti
# (265 KB, gizli/kişisel veri değil). Repoda tutulmazsa temiz bir clone'da
# `python backend/train_pipeline.py` koşamaz ve Faz 0'ın yeniden
# üretilebilirliği yalnızca tek bir makinede geçerli olur.

# Konsolide edilmiş model artefaktı commit EDİLİR.
# Gerekçe: backend Hugging Face Spaces'e Docker imajı olarak deploy ediliyor;
# imaj build'i sırasında model eğitilmiyor, repodan kopyalanıyor. Dosya ~165 KB,
# git için sorun değil. Yukarıdaki `*.pkl` kuralının istisnasıdır.
!backend/models/pipeline.pkl
