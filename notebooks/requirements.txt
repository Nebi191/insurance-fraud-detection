# FOR THE `notebooks/` EXPLORATION ENVIRONMENT ONLY — this is not production.
#
# This file lists the packages needed to re-run the exploration notebooks 01-05
# (EDA, preprocessing experiments, LightGBM/XGBoost comparison, Optuna HPO, SHAP
# analysis). It was moved here from the repository root as
# `notebooks/requirements.txt`; while it sat at the root it kept being confused
# with the backend's real dependency file.
#
# THE BACKEND DOES NOT USE THIS FILE.
# The backend (FastAPI + pipeline.pkl) uses its own:
#     backend/requirements.txt   -> everything pinned with `==`
# Those pins are critical for `pipeline.pkl` compatibility; the loose `>=` ranges
# here must NEVER be carried into that file. The Docker image is likewise built
# only from backend/requirements.txt.
#
# The ranges here are deliberately loose: the notebooks are exploratory and do
# not produce artifacts (the published model is produced by
# `backend/train_pipeline.py`), so they do not need an exact version lock.
#
# NOTE: `streamlit` was removed — the Streamlit prototype at the repository root
# (`app.py`) was deleted and replaced by the FastAPI backend + React frontend.

# --- Data / model ---
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
lightgbm>=4.0
xgboost>=2.0          # notebook 04, comparison against LightGBM
optuna>=3.0           # notebook 03, TPE hyperparameter optimisation
joblib>=1.3

# --- Explainability ---
# In the notebooks, SHAP charts are drawn with matplotlib. In the backend SHAP is
# served as raw JSON and matplotlib is DELIBERATELY not installed — that is the
# difference.
shap>=0.44

# --- Visualisation ---
matplotlib>=3.7
seaborn>=0.12

# --- Notebook runtime ---
jupyter>=1.0
