# SADECE `notebooks/` keşif ortamı içindir — production değildir.
#
# Bu dosya, 01-05 numaralı keşif notebook'larını (EDA, preprocessing denemeleri,
# LightGBM/XGBoost karşılaştırması, Optuna HPO, SHAP analizi) yeniden koşabilmek
# için gereken paketleri listeler. Repo kökünden `notebooks/requirements.txt`
# olarak buraya taşındı; kökte durduğu sürece backend'in gerçek bağımlılık
# dosyasıyla karıştırılıyordu.
#
# BACKEND BU DOSYAYI KULLANMAZ.
# Backend (FastAPI + pipeline.pkl) kendi dosyasını kullanır:
#     backend/requirements.txt   -> hepsi `==` ile pinli
# Oradaki pinler `pipeline.pkl` pickle uyumluluğu için kritiktir; buradaki
# gevşek `>=` aralıkları o dosyaya ASLA taşınmamalı. Docker imajı da yalnızca
# backend/requirements.txt'ten kurulur.
#
# Buradaki aralıklar bilerek gevşek: notebook'lar keşif amaçlı, artefakt
# üretmiyorlar (yayınlanan model `backend/train_pipeline.py` tarafından
# üretilir), dolayısıyla tam sürüm kilidi gerektirmiyorlar.
#
# NOT: `streamlit` çıkarıldı — kökteki Streamlit prototipi (`app.py`) silindi,
# yerini FastAPI backend + React frontend aldı.

# --- Veri / model ---
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
lightgbm>=4.0
xgboost>=2.0          # notebook 04, LightGBM ile karşılaştırma
optuna>=3.0           # notebook 03, TPE hiperparametre optimizasyonu
joblib>=1.3

# --- Explainability ---
# Notebook'ta SHAP grafikleri matplotlib ile çizilir. Backend'de ise SHAP ham
# JSON olarak servis edilir ve matplotlib BİLEREK kurulmaz — fark burada.
shap>=0.44

# --- Görselleştirme ---
matplotlib>=3.7
seaborn>=0.12

# --- Notebook çalıştırma ortamı ---
jupyter>=1.0
