node_modules
dist
dist-ssr
*.local
.vite

# TypeScript incremental build cache — machine specific, never committed.
*.tsbuildinfo

# Netlify (Phase 7)
.netlify

# Environment variables — VITE_API_URL lives here, never committed.
.env
.env.local
.env.*.local
