node_modules
dist
dist-ssr
*.local
.vite

# TypeScript artımlı derleme önbelleği — makineye özgü, repoya girmez.
*.tsbuildinfo

# Netlify (Faz 7)
.netlify

# Ortam değişkenleri — VITE_API_URL burada tutulur, repoya girmez.
.env
.env.local
.env.*.local
